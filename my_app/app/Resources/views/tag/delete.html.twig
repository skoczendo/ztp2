{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.delete_tag'|trans }}
{% endblock %}

{% block body %}

    <h1>
        {{ 'title.delete_tag'|trans }}
    </h1>

    {% include '_form_with_id.html.twig' with {action_url: 'tag_delete', variable_id: tag.id} %}

    <div class="btn-group">
        <a href="{{ path('tag_index') }}" class="btn btn-default" title="{{ 'action.index'|trans }}">
            {{ 'action.index'|trans }}
        </a>
    </div>

{% endblock %}