{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.add_bookmark'|trans }}
{% endblock %}

{% block body %}

    <h1>
        {{ 'title.add_bookmark'|trans }}
    </h1>

    {% include '_form.html.twig' with {action_url: 'bookmark_add'} %}

    <div class="btn-group">
        <a href="{{ path('bookmark_index') }}" class="btn btn-default" title="{{ 'action.index'|trans }}">
            {{ 'action.index'|trans }}
        </a>
    </div>

{% endblock %}